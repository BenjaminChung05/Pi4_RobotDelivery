Place your TFLite symbol classifier assets here.

Expected files:
- `symbol_classifier.tflite`
- `labels.txt`

The default code expects classification labels such as `stop`, `left`, `right`, `slow`, or `go`.
If your model uses different names, update `symbol_detect.py` label aliases.
